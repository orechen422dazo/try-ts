# TypeScript with volta environment

1. setup
```shell
npm init -y
npm install typescript --save-dev
npx tsc --init
```

2. create new file
```shell
touch index.ts
```

3. run tsc command
```shell
npx tsc
```